# 🐰 วิธีเพิ่ม Logo ลงในแอป Math Kid's

## 📁 ขั้นตอนที่ 1: เตรียมรูปภาพ

1. เตรียมรูปภาพ logo ของคุณ (แนะนำเป็นรูปสี่เหลี่ยมจัตุรัส)
2. บันทึกเป็นไฟล์ชื่อ `logo.png`
3. รองรับไฟล์: `.png`, `.jpg`, `.jpeg`, `.gif`, `.webp`

## 📂 ขั้นตอนที่ 2: วางไฟล์

วางไฟล์ `logo.png` ลงในโฟลเดอร์:
```
appmath/assets/images/logo.png
```

## ⚙️ ขั้นตอนที่ 3: เปิดใช้งาน

เปิดไฟล์ `lib/main.dart` และแก้ไขบรรทัดที่ 16:

```dart
// เปลี่ยนจาก
const bool useLogo = false;

// เป็น
const bool useLogo = true;
```

## 🔄 ขั้นตอนที่ 4: รีสตาร์ทแอป

```bash
# หยุดแอปที่กำลังรัน (กด Ctrl+C)
# รันใหม่
cd appmath
flutter run
```

## 💡 หมายเหตุ

- ถ้าไม่มีรูปภาพหรือโหลดไม่ได้ แอปจะแสดง emoji 🐰 แทนอัตโนมัติ
- รูปภาพจะถูกครอบเป็นวงกลมโดยอัตโนมัติ
- ขนาดที่แสดงในแต่ละหน้าจอ:
  - Splash Screen: 115px
  - Auth Screen: 98px  
  - Main Menu: 80px
  - Success Screen: 95px
  - Summary Screen: 100px

## 🎨 การเปลี่ยนชื่อไฟล์

ถ้าต้องการใช้ชื่อไฟล์อื่น แก้ไขบรรทัดที่ 15 ใน `lib/main.dart`:

```dart
const String logoPath = 'assets/images/ชื่อไฟล์ของคุณ.png';
```

